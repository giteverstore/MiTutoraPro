# Documentation lifecycle

**Status: CURRENT**

Architecture documents declare **CURRENT**, **LEGACY**, **DEPRECATED**, or **PLANNED**. Current documents link to canonical implementations. Legacy code must not be described as production authority. `npm run validate:documentation` checks local Markdown links; semantic accuracy still requires human review.
