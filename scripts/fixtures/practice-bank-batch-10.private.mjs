// Validator-only material. This module is never imported by the browser bundle.
export const referenceImplementations = Object.freeze({
  'fund-errors-001': "def safe_decimal_value(text):\n    value = text.strip()\n    if value.startswith(('+', '-')):\n        value = value[1:]\n    parts = value.split('.')\n    valid = len(parts) == 1 and parts[0].isdigit()\n    if len(parts) == 2:\n        valid = parts[0].isdigit() and parts[1].isdigit()\n    if not valid:\n        return None\n    return float(text.strip())",
  'fund-errors-002': 'def safe_ratio(numerator, denominator):\n    if denominator == 0:\n        return None\n    return numerator / denominator',
  'fund-errors-003': "def safe_quantity(text):\n    value = text.strip()\n    if not value or not value.isdigit():\n        return 0\n    return int(value)",
  'fund-errors-004': 'def configuration_value(config, key, fallback):\n    if key in config:\n        return config[key]\n    return fallback',
  'fund-errors-005': "def validated_grade(score):\n    if score < 0 or score > 100:\n        return 'Invalid'\n    if score >= 60:\n        return 'Pass'\n    return 'Retry'",
  'fund-errors-006': 'def validated_average(scores):\n    if not scores:\n        return None\n    for score in scores:\n        if score < 0 or score > 100:\n            return None\n    return sum(scores) / len(scores)',
  'fund-errors-007': "def process_product_record(record):\n    if 'name' not in record or 'quantity' not in record:\n        return 'Invalid record'\n    name = record['name']\n    quantity = record['quantity']\n    if not isinstance(name, str) or not name.strip():\n        return 'Invalid record'\n    if type(quantity) is not int or quantity < 0:\n        return 'Invalid record'\n    return f'{name.strip()}: {quantity}'",
  'fund-errors-008': 'def positive_expense_total(expenses):\n    total = 0\n    for expense in expenses:\n        if expense > 0:\n            total += expense\n    return total',
  'fund-errors-009': "def pass_status(score):\n    if score >= 60:\n        return 'Pass'\n    return 'Retry'",
  'fund-errors-010': 'def nonempty_username_count(usernames):\n    count = 0\n    for username in usernames:\n        if username.strip():\n            count += 1\n    return count',
  'fund-errors-011': 'def category_total_or_zero(totals, category):\n    if category in totals:\n        return totals[category]\n    return 0',
  'fund-errors-012': "def valid_label_record_count(records):\n    count = 0\n    for record in records:\n        if record.count(':') != 1:\n            continue\n        left, right = [field.strip() for field in record.split(':')]\n        if left and right:\n            count += 1\n    return count",
  'fund-errors-013': "def valid_transaction_total(transactions):\n    def decimal_value(text):\n        value = text.strip()\n        if value.startswith(('+', '-')):\n            value = value[1:]\n        parts = value.split('.')\n        if len(parts) == 1 and parts[0].isdigit():\n            return float(text.strip())\n        if len(parts) == 2 and parts[0].isdigit() and parts[1].isdigit():\n            return float(text.strip())\n        return None\n    total = 0\n    for transaction in transactions:\n        if transaction.count(':') != 1:\n            continue\n        name, amount_text = [field.strip() for field in transaction.split(':')]\n        if not name:\n            continue\n        amount = decimal_value(amount_text)\n        if amount is not None:\n            total += amount\n    return total",
  'fund-errors-014': 'def clean_score_inputs(score_texts):\n    result = set()\n    for text in score_texts:\n        value = text.strip()\n        if value.isdigit():\n            score = int(value)\n            if 0 <= score <= 100:\n                result.add(score)\n    return sorted(result)',
  'fund-errors-015': "def category_amount_summary(records):\n    totals = {}\n    for record in records:\n        if record.count(':') != 1:\n            continue\n        category, amount_text = [field.strip() for field in record.split(':')]\n        if not category or not amount_text.isdigit():\n            continue\n        amount = int(amount_text)\n        totals[category] = totals.get(category, 0) + amount\n    return totals",
  'fund-errors-016': "def username_summary(usernames):\n    accepted = []\n    for username in usernames:\n        value = username.strip()\n        if value:\n            accepted.append(value)\n    return {'accepted': len(accepted), 'unique': len(set(accepted))}",
  'fund-errors-017': 'def reward_points(purchases):\n    points = 0\n    for purchase in purchases:\n        if purchase < 0:\n            continue\n        if purchase >= 10:\n            points += 2\n        else:\n            points += 1\n    return points',
  'fund-errors-018': 'def inventory_after_updates(inventory, updates):\n    result = dict(inventory)\n    for product, change in updates:\n        result[product] = result.get(product, 0) + change\n    return result',
  'fund-errors-019': 'def highest_valid_score(score_texts):\n    highest = None\n    for text in score_texts:\n        value = text.strip()\n        if not value.isdigit():\n            continue\n        score = int(value)\n        if score < 0 or score > 100:\n            continue\n        if highest is None or score > highest:\n            highest = score\n    return highest',
});

export const protectedTests = Object.freeze({
  'fund-errors-001': [{ arguments: ['+0'], expected: 0.0 }, { arguments: ['-.5'], expected: null }],
  'fund-errors-002': [{ arguments: [0, 5], expected: 0.0 }, { arguments: [-9, 3], expected: -3.0 }],
  'fund-errors-003': [{ arguments: [''], expected: 0 }, { arguments: ['4.5'], expected: 0 }],
  'fund-errors-004': [{ arguments: [{ note: '' }, 'note', 'none'], expected: '' }, { arguments: [{ count: 0 }, 'count', 7], expected: 0 }],
  'fund-errors-005': [{ arguments: [0], expected: 'Retry' }, { arguments: [-1], expected: 'Invalid' }],
  'fund-errors-006': [{ arguments: [[0, 100]], expected: 50.0 }, { arguments: [[50, -1]], expected: null }],
  'fund-errors-007': [{ arguments: [{ name: 'X', quantity: -1 }], expected: 'Invalid record' }, { arguments: [{ name: '   ', quantity: 0 }], expected: 'Invalid record' }],
  'fund-errors-008': [{ arguments: [[]], expected: 0 }, { arguments: [[1.5, 2.5, -1]], expected: 4.0 }],
  'fund-errors-009': [{ arguments: [100], expected: 'Pass' }, { arguments: [0], expected: 'Retry' }],
  'fund-errors-010': [{ arguments: [[' ', 'X', 'Y']], expected: 2 }, { arguments: [['only']], expected: 1 }],
  'fund-errors-011': [{ arguments: [{ x: -2 }, 'x'], expected: -2 }, { arguments: [{}, 'none'], expected: 0 }],
  'fund-errors-012': [{ arguments: [['a:b:c', 'x:y']], expected: 1 }, { arguments: [[' : ', 'key:value']], expected: 1 }],
  'fund-errors-013': [{ arguments: [['A:+3', 'B:0.5', 'C:-.5']], expected: 3.5 }, { arguments: [[]], expected: 0 }],
  'fund-errors-014': [{ arguments: [['100', ' 0 ', '+4', '100']], expected: [0, 100] }, { arguments: [[]], expected: [] }],
  'fund-errors-015': [{ arguments: [['a:0', 'a:2', ':4']], expected: { a: 2 } }, { arguments: [['a:1:2', 'b:x']], expected: {} }],
  'fund-errors-016': [{ arguments: [[]], expected: { accepted: 0, unique: 0 } }, { arguments: [['A', 'a', 'A']], expected: { accepted: 3, unique: 2 } }],
  'fund-errors-017': [{ arguments: [[0, 9.9, 10]], expected: 4 }, { arguments: [[]], expected: 0 }],
  'fund-errors-018': [{ arguments: [{}, [['x', 1], ['x', 2]]], expected: { x: 3 } }, { arguments: [{ x: 0 }, [['x', -1]]], expected: { x: -1 } }],
  'fund-errors-019': [{ arguments: [['0', ' 99 ', '99']], expected: 99 }, { arguments: [[]], expected: null }],
});
