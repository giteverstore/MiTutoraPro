import { ArrowRight, Clock3, Code2 } from 'lucide-react';
export function ProjectCard({ project, progress, onOpen }) { return <article className="project-card"><div className="project-card-icon"><Code2 /></div><div><span>{project.difficulty} · {project.language}</span><h3>{project.title}</h3><p>{project.description}</p><ul>{project.skills.slice(0, 3).map((skill) => <li key={skill}>{skill}</li>)}</ul><footer><small><Clock3 /> {project.estimatedMinutes} min</small><button className="button button--primary" type="button" onClick={() => onOpen(project)}>{progress.status === 'completed' ? 'Review' : progress.status === 'started' ? 'Continue' : 'View Project'} <ArrowRight /></button></footer></div></article>; }
