import { CheckCircle2, CircleX } from 'lucide-react';
export function ProjectTestResults({ result }) { if (!result) return null; return <section className="project-test-results" aria-live="polite"><header><h2>Validation results</h2><strong>{result.score}%</strong></header>{result.errors?.length ? <div role="alert">{result.errors.join('\n')}</div> : <ul>{result.tests.map((test) => <li className={test.passed ? 'is-passed' : 'is-failed'} key={test.name}>{test.passed ? <CheckCircle2 /> : <CircleX />}<span><strong>{test.name}</strong><small>{test.message}</small>{test.visible && !test.passed ? <code>Expected {JSON.stringify(test.expected)} · Received {JSON.stringify(test.actual)}</code> : null}</span></li>)}</ul>}</section>; }
