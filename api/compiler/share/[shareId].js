import { assertAllowedOrigin, createCompilerPublicDependencies, readShare, sendCompilerPublicError } from '../../../server/compiler-public/compilerPublicService.js';
export default async function handler(request, response) { response.setHeader('Cache-Control', 'no-store'); try { if (request.method !== 'GET') return response.status(405).json({ error: { code: 'compiler-public/method-not-allowed', message: 'Method not allowed.' } }); assertAllowedOrigin(request); const dependencies = await createCompilerPublicDependencies(); return response.status(200).json(await readShare({ ...dependencies, shareId: request.query.shareId })); } catch (error) { return sendCompilerPublicError(response, error); } }
